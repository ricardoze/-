<Page x:Class="记账.Pages.Goods"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:记账.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">
        <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Button HorizontalAlignment="Left" Width="60px" Height="20px">添加</Button>
        </Grid>
        <Grid Grid.Row="1">
            <ScrollViewer>
                <DataGrid>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="商品名" Width="80" Binding="{Binding Name}"/>
                        <DataGridTextColumn Header="售价" Width="50" Binding="{Binding Price}"/>
                        <DataGridComboBoxColumn Width="80" Header="进价" 
                                        SelectedItemBinding="{Binding InPrice}" />
                        <DataGridCheckBoxColumn Header="备注" Width="100" 
                                        Binding="{Binding Remarks}"/>
                        <DataGridHyperlinkColumn Header="使用中" Width="150" 
                                         Binding="{Binding InUsed}"/>
                        <DataGridHyperlinkColumn Header="操作" Width="150" 
                                         Binding="{Binding InUsed}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
